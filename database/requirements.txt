# Версии зафиксированы (==) на тех, с которыми прогонялся полный набор тестов.
# Без пинов любой релиз SQLAlchemy или FastAPI мог молча сломать прод.
#
# Это не полноценный lock-файл: транзитивные зависимости не зафиксированы.
# Для побайтовой воспроизводимости нужен pip-tools/uv — см. plans/fix-known-gaps.md.
#
# Тестовые зависимости вынесены в requirements-dev.txt: на проде они не нужны.

# --- Миграции ---

alembic==1.18.3
sqlalchemy==2.0.46
pymysql==1.1.2

# Нужна PyMySQL для SSL-подключений
cryptography==46.0.4
