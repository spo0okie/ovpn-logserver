# Зависимости только для запуска тестов. На проде не нужны.
# Ставить вместе с прод-зависимостями:
#   pip install -r web/requirements.txt -r collector/requirements.txt #               -r database/requirements.txt -r requirements-dev.txt

pytest==9.0.2
pytest-asyncio==1.3.0

# Фикстура mocker используется примерно в половине тестов, но раньше
# нигде не объявлялась — на чистом окружении сборка падала
pytest-mock==3.15.1

# Клиент для TestClient FastAPI
httpx==0.28.1
