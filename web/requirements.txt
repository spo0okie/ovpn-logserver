# Версии зафиксированы (==) на тех, с которыми прогонялся полный набор тестов.
# Без пинов любой релиз SQLAlchemy или FastAPI мог молча сломать прод.
#
# Это не полноценный lock-файл: транзитивные зависимости не зафиксированы.
# Для побайтовой воспроизводимости нужен pip-tools/uv — см. plans/fix-known-gaps.md.
#
# Тестовые зависимости вынесены в requirements-dev.txt: на проде они не нужны.

# --- Web-приложение (FastAPI) ---

fastapi==0.128.0
jinja2==3.1.6
python-multipart==0.0.22

# ASGI-сервер. Версия НЕ зафиксирована: тестами не покрывается (тесты идут
# через TestClient), поэтому «проверенной» версии у него нет. Ограничиваем
# сверху мажором, чтобы breaking change не приехал незаметно.
uvicorn[standard]>=0.24.0,<1.0

# --- Работа с БД ---
sqlalchemy==2.0.46
pymysql==1.1.2

# --- Валидация и конфигурация ---
pydantic==2.12.5
pyyaml==6.0.3

# --- Аутентификация ---
passlib[bcrypt]==1.7.4
bcrypt==5.0.0
cryptography==46.0.4

# --- Прочее ---
requests==2.32.5
tzlocal==5.3.1
