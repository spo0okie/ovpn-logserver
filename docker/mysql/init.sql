-- =============================================================================
-- OpenVPN LogServer - MySQL bootstrap (Docker)
-- =============================================================================
-- Выполняется при ПЕРВОМ старте mysql-контейнера (пустой том).
--
-- ВАЖНО: таблицы здесь НЕ создаются. Схема — единый источник правды: миграции
-- Alembic. web-контейнер на старте выполняет `alembic upgrade head`
-- (см. docker/web/entrypoint.sh) и создаёт/мигрирует все таблицы сам.
--
-- Раньше этот файл создавал таблицы параллельно с Alembic — на пустом томе
-- миграция 001 падала с "Table already exists", и web уходил в crash-loop.
-- Теперь конфликта нет: MySQL-контейнер создаёт саму БД (env MYSQL_DATABASE),
-- а таблицы — только Alembic.
-- =============================================================================

-- Приводим БД к нужной кодировке/сортировке (имя БД задаётся env MYSQL_DATABASE;
-- по умолчанию в docker-compose — openvpn_logs).
ALTER DATABASE openvpn_logs CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
